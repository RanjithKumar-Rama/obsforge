# Local MinIO bucket config for docker-compose.
# Do NOT use this in production — it has no TLS and uses default credentials.

type: S3
config:
  bucket: thanos
  endpoint: minio:9000
  region: us-east-1
  access_key: minioadmin
  secret_key: minioadmin
  insecure: true
